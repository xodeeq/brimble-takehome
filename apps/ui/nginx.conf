server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Required for client-side routing — all paths fall back to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
